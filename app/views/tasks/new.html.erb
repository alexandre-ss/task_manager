<h1 class = "text-center"> New Task</h1>
<div class = "container d-flex">
  <div class = "row mx-auto">
    <div class = "card">
      <div class="card-body">
        <%= form_for( @task , remote: true) do |f| %>
          <div class = "form-group">
            <%= f.label :priority, "Task Title"%>
            <%= f.text_field :title, placeholder: "Task Title", class: "form-control" %>
          </div>

          <div class = "form-group">
            <%= f.label :priority, "Description"%>
            <%= f.text_area :description, placeholder: "Description", class: "form-control" %>
          </div>

          <div>
            <%= f.label :share, "Privacy"%>
            <%= f.select :share, options_for_select([["Public",true], ["Private", false]]),{}, class: "custom-select mb-2" %>
          </div>

          <div>
            <%= f.label :priority, "Priority"%>
            <%= f.select :priority, options_for_select(Task.priorities.map { |key, value| [key.humanize, key] }),{}, class: "custom-select mb-2" %>
          </div>
          
          <%= f.hidden_field :user_id, value: current_user.id %> 
          <%= f.submit "Create Task", class: "btn btn-secondary"%>
        <% end %>            
      </div>
    </div>
  </div>
</div>